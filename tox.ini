[tox]
envlist =
    check,
    py36,
    py37,
    docs

[testenv]
deps = pytest
commands = pytest tests

[testenv:check]
deps =
    black
    flake8
    flake8-bugbear
    isort
skip_install = true
commands =
    black . --check
    flake8 .
    isort --multi-line=3 --trailing-comma --force-grid-wrap=0 --use-parentheses --line-width=88 --check-only --diff --recursive src tests docs setup.py

[testenv:docs]
deps =
    -r{toxinidir}/docs/requirements.txt
commands =
    sphinx-build {posargs:-E} -b html docs dist/docs
    sphinx-build -b linkcheck docs dist/docs

[flake8]
max_line_length = 80
select = B,C,E,F,W,T4,B9
ignore = E501,W503,E2030
